<nav class="navbar navbar-expand-lg sticky-top">
  <div class="container-fluid" style="max-width:1360px; margin:0 auto;">
    <a class="wl-brand" href="/listings">
      <i class="fa-regular fa-compass"></i> Roamly
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="wl-nav-links">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="navbar-nav ms-auto mx-lg-auto my-3 my-lg-0">
        <form class="wl-search" role="search">
          <i class="fa-solid fa-magnifying-glass" style="color:var(--color-gray-500); font-size:0.85rem;"></i>
          <input class="search-input" type="search" placeholder="Search destinations" style="padding-left:0.6rem !important;">
          <button class="search-btn" type="submit" aria-label="Search"><i class="fa-solid fa-magnifying-glass"></i><span>Search</span></button>
        </form>
      </div>

      <div class="wl-nav-links ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(typeof currUser === "undefined" || !currUser) { %>
        <a class="nav-link nav-cta nav-signup" href="/signup">Sign Up</a>
        <a class="nav-link nav-cta" href="/login">Login</a>
        <% } %>
        <% if(typeof currUser !== "undefined" && currUser) { %>
        <div class="wl-user-chip">
          <div class="avatar-circle avatar-sm"><%= currUser.username.charAt(0).toUpperCase() %></div>
          <span><%= currUser.username %></span>
        </div>
        <a class="nav-link nav-cta" href="/logout">Log Out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>